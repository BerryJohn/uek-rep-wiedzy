import React from "react";
import "./TopBar.css";

const TopBar: React.FC = () => {
  return (
    <header className="topbar">
      <h1 className="topbar-title">BookPol </h1>
    </header>
  );
};

export default TopBar;
